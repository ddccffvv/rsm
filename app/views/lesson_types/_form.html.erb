<%= form_for(@lesson_type) do |f| %>
  <% if @lesson_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson_type.errors.count, "error") %> prohibited this lesson_type from being saved:</h2>

      <ul>
      <% @lesson_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hourly_rate %><br />
    <%= f.text_field :hourly_rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
